[property]
gpu-id=0
gie-unique-id=2

# Secondary GIE mode (operates on detected objects/faces)
process-mode=2

# Operate on faces detected by PGIE (gie-id=1)
operate-on-gie-id=1

# TensorRT engine with batch support (1-32 faces)
model-engine-file=/home/tempuser/Downloads/Frs_sec/models/tensorrt/arcface_r50_batch.engine
onnx-file=/home/tempuser/Downloads/Frs_sec/models/tensorrt/arcface_r50_dynamic.onnx

# Input preprocessing for ArcFace
# ArcFace expects: (x - 127.5) / 127.5 = x/127.5 - 1
net-scale-factor=0.0078431372549
offsets=127.5;127.5;127.5
model-color-format=0

# Input dimensions (112x112 aligned face)
infer-dims=3;112;112

# Batch size for SGIE (processes all detected faces in batch)
batch-size=32

# FP16 inference
network-mode=2

# Custom network (embedding output, not classifier)
network-type=100
num-detected-classes=1

# Output raw embeddings as tensor metadata
output-tensor-meta=1

# Maintain aspect ratio (face crops are already aligned)
maintain-aspect-ratio=0

# Process on GPU
process-mode=2
gpu-id=0

# Workspace
workspace-size=2048

# Classifier async mode for better throughput
classifier-async-mode=1

[class-attrs-all]
pre-cluster-threshold=0.0
